{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rmdir /s /q dist && mkdir dist && cd ../../part2/phonebook && npm run build && xcopy dist ..\\..\\part3\\phonebook-backend\\dist /E /I",
    "deploy:full": "npm run build:ui && git add . && git commit -m Removing-dist-from-gitignore && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "morgan": "^1.10.0",
    "nodemon": "^3.0.1"
  }
}
